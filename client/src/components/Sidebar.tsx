import React from 'react';

import { Paper } from '@mui/material';

export const Sidebar = (props: any) => (
  <div data-testid='collapsible-sidebar'>
    <div data-testid='toggle-drawer'>ou deveria ser um botão</div>
    eu deveria ser uma sidebar
    <Paper>eu deveria ser um paper</Paper>
  </div>
);
